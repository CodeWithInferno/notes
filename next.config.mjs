// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
export const nextConfig = {
  images: {
    domains: ['ocgiyhicvoovieaunvar.supabase.co'],
  },
};

export default nextConfig;
