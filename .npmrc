# 🚀 Force NPM to stop crying about peer dependency mismatches
legacy-peer-deps=true

# 🧪 Optional: Make installs faster by skipping audit during deploy
audit=false

# 🚫 Optional: Don’t fund NPM packages during CI
fund=false
